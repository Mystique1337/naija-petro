[pytest]
# Fast, hermetic unit suite: no network, no database, no GPU, no .env.
testpaths = tests
pythonpath = .
python_files = test_*.py
addopts = -q --strict-markers --strict-config --tb=short -p no:cacheprovider
